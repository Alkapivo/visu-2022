
	if (connectedTimer > 0) {
		connectedTimer -= 1;
		
		/*
		var outlineColor = c_black;
		var textColor = make_color_rgb(68, 255, 34);
		draw_set_font(font_tandy_tv2y);
		draw_set_halign(fa_center);
		*/
		//drawTextOutline(GuiWidth / 2, GuiHeight / 2, outlineColor, textColor, "CONNECTED");
	}

	
	